# noop

Performs just the required operations by the communication protocol.  Does no other action.

This ensures that the process passes all sandbox checks, as it doesn't attempt any out-of-band actions.
