# clipboard

Uses several different libraries to poll for different mechanisms that identify the computer.

The invocation reads an argument, which it interprets as the number of times to read the clipboard.  It will rest for 1 second between reads.
