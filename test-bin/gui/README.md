# GUI

Attempts to launch a user interface.  The wrapping program should prevent the interface from starting.

If you're on Linux using Wayland, you can force this to run as an X11 program by setting `WAYLAND_DISPLAY=` and `WAYLAND_SOCKET=`.
